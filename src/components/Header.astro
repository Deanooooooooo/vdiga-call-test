---
import { site } from "../data/site";

const basePath = import.meta.env.BASE_URL.replace(/\/$/, "");
const href = (path: string) => `${basePath}${path}`;
---

<header class="site-top">
  <div class="container nav">
    <a class="brand" href={href("/")} aria-label="Vdiga начало">
      <img class="brand-logo" src={href("/assets/vdiga-logo-nav-green-v.png?v=259afb18")} alt="Vdiga" width="320" height="123" />
    </a>
    <nav class="nav-links" aria-label="Основна навигация">
      {site.nav.map((item) => <a href={href(item.href)}>{item.label}</a>)}
    </nav>
    <div class="nav-actions">
      <a class="btn btn-secondary nav-cta nav-login" href={href("/login")}>Вход</a>
      <a class="btn btn-primary nav-cta nav-signup" href={href("/signup")}>Регистрация</a>
      <a class="btn btn-primary nav-cta nav-demo" href={href("/demo")}>Чуйте разговор</a>
    </div>
  </div>
</header>
